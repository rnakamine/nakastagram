<h1>Edit</h1>
<% @post.errors.full_messages.each do |message| %>
  <% if message == "Content can't be blank" %>
    <p style="color:red;font-size:12px;">・内容を入力してください</p>
  <% end %>
<% end %>
<form action="/edit/<%= @post.id %>" method="post">
  <p><input type="text" name="content" value="<%= @post.content %>"></p>
  <p><input type="submit" value="Edit"></p>
</form>
<a href="/post/<%= @post['id'] %>">Back</a>